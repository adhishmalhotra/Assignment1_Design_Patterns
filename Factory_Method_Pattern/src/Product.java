//This is the product interface. It shall be used to create objects without specifying the exact class to create the objects.
interface Product {
    String ProductName();
    double ProductPrice(double price);
}
